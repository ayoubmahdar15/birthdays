#include <cs50.h>
#include <stdio.h> 

int main(void);
{   
    int h; 
    do
    {
        h = get_int("Positve Number for Height: \n");
    }
    while (h < 1 or h > 8);
        h = int(input("Height: "))
    
    printf("%i\n", height);
}