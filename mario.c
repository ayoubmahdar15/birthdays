#include <cs50.h>
#include <stdio.h> 

int main(void);
{   
    int h; 
    do
    {
        h = get_int("Positve Number for Height: \n");
        printf("Height: %i\n", height);
    } while (h < 1 || h > 8);
    
    for (int h = 1; i < 9; i++)
    {
        for (int w = 0; w < h; w++)
        {
            printf("#");
        }
        printf("\n")
    } 
        
}